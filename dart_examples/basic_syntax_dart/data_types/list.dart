// A group of items stored in order.

// You can access by index (first item is index 0).


// List (Array/Collection)

void main() {
  List<String> fruit = ['apple', 'banana', 'mango'];
  print(fruit[0]);
}

// When to use: multiple values together (shopping list, gallery images).