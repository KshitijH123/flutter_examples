// Only two values: true or false.

// Useful for conditions and switches.

void main() {
  bool isLoggedIn = true;
  bool hasError = false;

  print(isLoggedIn);
  print(hasError);
}

// When to use: yes/no questions, toggle settings, if-else conditions.