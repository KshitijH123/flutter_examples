// If you want to do some calculation or method call inside the String, use ${}.

void main() {
  int a = 10;
  int b = 20;

  print('Sum is ${a + b}');

}


// Important:
// For simple variables, $variableName is enough.

// For operations/methods, always use ${}.